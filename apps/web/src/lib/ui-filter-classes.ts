/**
 * Shared Tailwind classes for list filter toolbars (date inputs, etc.)
 * so partner pages stay visually aligned with shadcn Input tokens.
 * Select filters use `FilterSelect` (`@/components/ui/filter-select`).
 */
export const DATE_INPUT_FILTER =
  "flex h-11 rounded-full border border-input bg-background px-3.5 py-2 text-[17px] tracking-[-0.374px] ring-offset-background focus-visible:outline-none focus-visible:border-primary focus-visible:shadow-[var(--focus-ring)] disabled:cursor-not-allowed disabled:opacity-50";
